# Screen-Level Visual Regression Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pixel-compare three key screens of the real Electron shell against committed per-platform baselines on all three CI legs, with a dispatchable workflow that regenerates the baselines in CI and commits them.

**Architecture:** A fifth Playwright project (`visual`) reuses the existing Electron launch fixture and asserts `toHaveScreenshot` on three screens. A `snapshotPathTemplate` forces a `{platform}` suffix so explicitly named snapshots never collide across OSes. Baselines are only ever produced by `update-visual-baselines.yml` (workflow_dispatch, 3-OS matrix, single bot commit to the calling branch) because Playwright requires baselines from the environment where comparisons run.

**Tech Stack:** Playwright 1.61 (`toHaveScreenshot`, pixelmatch), playwright-bdd fixtures (reused, no Gherkin), GitHub Actions 3-OS matrix, plain-git PNG baselines.

**Spec:** `docs/superpowers/specs/2026-07-25-e2e-visual-design.md`

## Global Constraints

- Never commit to `main`; all work happens on branch `worktree-e2e-visual` in this worktree; one PR closes the job.
- The forbidden owner alias (the word the gitleaks `forbidden-owner-alias` rule bans) must never appear in any artifact.
- No code comments. No em dashes in authored prose. No `as` casts. TypeScript max strictness.
- Every commit message follows caveman-commit and ends with the trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- The PR body ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
- Full three-platform matrix from day one (maintainer directive): baselines exist for darwin, win32, and linux, and every leg asserts.
- Baselines come ONLY from CI runners, never from a local machine (spec: "the continuous-integration runners are the only source of truth for every platform, including macOS"). Never commit locally generated PNGs.
- The tolerance is explicit in config: start at `maxDiffPixels: 0`; raise only with observed CI evidence, recorded in the ADR (day-one-green rule).
- Workflow actions pinned by full commit SHA with trailing version comment, matching `ci.yml`.
- Existing acceptance, proofs, leak, and packaged projects stay untouched.
- ADRs and docs pass Vale (Microsoft at error) + cspell; plans (this file) are exempt.

## File structure

| File                                                                     | Responsibility                                                                          |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| `apps/desktop/e2e/visual.spec.ts`                                        | Three screen assertions against the launched Electron shell                             |
| `apps/desktop/e2e/playwright.config.ts`                                  | `visual` project, `snapshotPathTemplate`, explicit `maxDiffPixels`                      |
| `apps/desktop/package.json`                                              | `test:e2e:visual` and `test:e2e:visual:update` scripts; visual joins the quarantine run |
| `.github/workflows/ci.yml`                                               | visual step on all three `e2e` legs (linux inside the existing dbus/keyring script)     |
| `.github/workflows/update-visual-baselines.yml`                          | Dispatchable 3-OS regeneration + single bot commit to the branch                        |
| `apps/desktop/e2e/visual.spec.ts-snapshots/`                             | Nine committed PNGs (arrive via the workflow's bot commit, never authored locally)      |
| `docs/adr/0034-screen-level-visual-regression.md` + `docs/adr/README.md` | Decision record + index row                                                             |

**Parallelizable:** Tasks 1, 2, 3 touch disjoint files. Task 4 needs all three pushed plus the PR.

---

### Task 1: The visual project and spec

**Files:**

- Create: `apps/desktop/e2e/visual.spec.ts`
- Modify: `apps/desktop/e2e/playwright.config.ts`
- Modify: `apps/desktop/package.json` (scripts)
- Modify: `.github/workflows/ci.yml` (e2e job steps)

**Interfaces:**

- Consumes: `test` from `./fixtures` (playwright-bdd extended test carrying `electronApp` and `page` fixtures; `page` is the app's first window, `domcontentloaded` awaited, fresh temp user-data dir per test).
- Produces: the `visual` Playwright project and the `test:e2e:visual` / `test:e2e:visual:update` scripts Task 2's workflow and Task 4's verification rely on. Snapshot names: `home-empty`, `providers-empty`, `providers-connected`, stored as `{name}-{platform}.png`.

- [ ] **Step 1: Write the spec (it is the failing test — no baselines exist yet)**

`apps/desktop/e2e/visual.spec.ts`:

```ts
import type { Page } from '@playwright/test';

import { expect } from '@playwright/test';

import { test } from './fixtures';

async function settleFonts(page: Page): Promise<void> {
  await page.evaluate(async () => {
    await document.fonts.ready;
  });
}

async function openProviders(page: Page): Promise<void> {
  await page.getByRole('link', { name: 'Providers' }).click();
  await expect(page.getByRole('heading', { name: 'Providers' })).toBeVisible();
}

test('the home screen matches its baseline', async ({ page }) => {
  await expect(page.getByText('Select a gateway or create one to get started.')).toBeVisible();
  await settleFonts(page);
  await expect(page).toHaveScreenshot('home-empty.png');
});

test('the providers screen matches its baseline before any account exists', async ({ page }) => {
  await openProviders(page);
  await expect(page.getByRole('listitem')).toHaveCount(0);
  await settleFonts(page);
  await expect(page).toHaveScreenshot('providers-empty.png');
});

test('the providers screen matches its baseline with a connected account', async ({ page }) => {
  await openProviders(page);
  await page.getByRole('textbox', { name: 'Provider' }).fill('anthropic');
  await page.getByRole('combobox', { name: 'Kind' }).selectOption('api-key');
  await page.getByRole('textbox', { name: 'Label' }).fill('Claude Max');
  await page.getByRole('textbox', { name: 'Secret' }).fill('not-a-real-secret');
  await page.getByRole('button', { name: 'Connect' }).click();
  await expect(page.getByRole('listitem').filter({ hasText: 'Claude Max' })).toBeVisible();
  await settleFonts(page);
  await expect(page).toHaveScreenshot('providers-connected.png');
});
```

The window is deterministic at 900×670 (`windowOptionsFor` in `apps/desktop/src/main/windows/window-options.ts`); do not resize it. The connect flow copies the acceptance step's exact locators (`apps/desktop/e2e/steps/providers.steps.ts`).

- [ ] **Step 2: Register the project, the path template, and the tolerance**

In `apps/desktop/e2e/playwright.config.ts`, extend the `defineConfig` object (keep everything already there):

```ts
export default defineConfig({
  timeout: 30_000,
  retries: process.env['CI'] === undefined ? 0 : 2,
  use: { trace: 'on-first-retry' },
  snapshotPathTemplate: '{testFileDir}/{testFileName}-snapshots/{arg}-{platform}{ext}',
  expect: { toHaveScreenshot: { maxDiffPixels: 0 } },
  projects: [
    { name: 'acceptance', testDir: acceptanceDir },
    { name: 'proofs', testMatch: /boot-proof\.spec\.ts/ },
    { name: 'leak', testMatch: /leak\.spec\.ts/ },
    { name: 'packaged', testMatch: /packaged-smoke\.spec\.ts/, timeout: 120_000 },
    { name: 'visual', testMatch: /visual\.spec\.ts/ },
  ],
});
```

The `snapshotPathTemplate` is load-bearing: Playwright does NOT suffix explicitly named snapshots, so without it `home-empty.png` would collide across the three platforms.

- [ ] **Step 3: Add the scripts**

In `apps/desktop/package.json` scripts, add these two and extend the quarantine run:

```json
"test:e2e:visual": "playwright test -c e2e/playwright.config.ts --project visual --grep-invert @quarantine",
"test:e2e:visual:update": "playwright test -c e2e/playwright.config.ts --project visual --update-snapshots",
```

and change `test:e2e:quarantine` to include the visual project:

```json
"test:e2e:quarantine": "bddgen -c e2e/playwright.config.ts && playwright test -c e2e/playwright.config.ts --project acceptance --project proofs --project leak --project visual --grep @quarantine --pass-with-no-tests"
```

- [ ] **Step 4: Wire the CI steps**

In `.github/workflows/ci.yml`, inside the `e2e` job:

1. In the Linux heredoc script (`/tmp/e2e-linux.sh`, the block ending with `dbus-run-session -- bash /tmp/e2e-linux.sh`), append one line after the `test:e2e` line so the visual run shares the unlocked keyring session:

```bash
xvfb-run --auto-servernum pnpm --filter @recompose/desktop run test:e2e:visual
```

2. After the existing `- if: runner.os != 'Linux'` / `run: pnpm --filter @recompose/desktop run test:e2e` step, add:

```yaml
- if: runner.os != 'Linux'
  run: pnpm --filter @recompose/desktop run test:e2e:visual
```

The existing `if: failure()` trace-artifact upload already captures `apps/desktop/test-results`, which is where Playwright writes expected/actual/diff images on a visual failure — no new upload step.

- [ ] **Step 5: Verify mechanics locally WITHOUT committing baselines**

Run: `pnpm exec turbo run build` then `pnpm --filter @recompose/desktop run test:e2e:visual:update`
Expected: three tests write `apps/desktop/e2e/visual.spec.ts-snapshots/{home-empty,providers-empty,providers-connected}-darwin.png` and pass.

Run: `pnpm --filter @recompose/desktop run test:e2e:visual`
Expected: PASS against the just-written local baselines (proves the comparison path).

Then DELETE the local baselines — they must never be committed (CI is the only source of truth):

```bash
rm -rf apps/desktop/e2e/visual.spec.ts-snapshots
```

- [ ] **Step 6: Run gates and commit**

Run: `pnpm exec turbo run lint typecheck && pnpm run fmt:check`
Expected: PASS.

```bash
git add apps/desktop/e2e/visual.spec.ts apps/desktop/e2e/playwright.config.ts apps/desktop/package.json .github/workflows/ci.yml
git commit -m "test(e2e): visual assertions for three shell screens

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

Note for the reviewer: the branch intentionally carries no baseline PNGs after this task; the `e2e` legs stay red on the PR until Task 4 dispatches the update workflow.

---

### Task 2: The baseline-update workflow

**Files:**

- Create: `.github/workflows/update-visual-baselines.yml`

**Interfaces:**

- Consumes: the `test:e2e:visual:update` script from Task 1.
- Produces: a dispatchable workflow Task 4 runs to seed and refresh baselines.

- [ ] **Step 1: Resolve the download-artifact pin**

The repo already pins `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1`. Resolve the matching `actions/download-artifact` pin:

Run: `gh api repos/actions/download-artifact/tags --jq '.[] | "\(.name) \(.commit.sha)"' | head -5`
Use the newest stable major's exact tag SHA in Step 2 with a `# vX.Y.Z` comment.

- [ ] **Step 2: Create the workflow**

`.github/workflows/update-visual-baselines.yml` (all other action pins copied verbatim from `ci.yml`; `DOWNLOAD_SHA # vX.Y.Z` from Step 1):

```yaml
name: update-visual-baselines

on:
  workflow_dispatch:
    inputs:
      branch:
        description: branch whose visual baselines get regenerated
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: update-visual-baselines-${{ inputs.branch }}
  cancel-in-progress: true

jobs:
  regenerate:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
        if: runner.os == 'Linux'
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.branch }}
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile --trust-lockfile
      - run: pnpm --filter @recompose/desktop exec node node_modules/electron/install.js
      - run: pnpm exec turbo run build
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAMID: ${{ vars.TURBO_TEAMID }}
      - if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y gnome-keyring
      - if: runner.os == 'Linux'
        run: |
          cat > /tmp/visual-update-linux.sh <<'SCRIPT'
          set -e
          eval "$(printf '\n' | gnome-keyring-daemon --unlock)"
          eval "$(printf '\n' | gnome-keyring-daemon --start --components=secrets)"
          export XDG_CURRENT_DESKTOP=GNOME
          export RECOMPOSE_PASSWORD_STORE=gnome-libsecret
          xvfb-run --auto-servernum pnpm --filter @recompose/desktop run test:e2e:visual:update
          SCRIPT
          dbus-run-session -- bash /tmp/visual-update-linux.sh
      - if: runner.os != 'Linux'
        run: pnpm --filter @recompose/desktop run test:e2e:visual:update
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: visual-baselines-${{ runner.os }}
          path: apps/desktop/e2e/visual.spec.ts-snapshots/
          retention-days: 3

  commit:
    needs: regenerate
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.branch }}
          persist-credentials: false
      - uses: actions/download-artifact@DOWNLOAD_SHA # vX.Y.Z
        with:
          pattern: visual-baselines-*
          merge-multiple: true
          path: apps/desktop/e2e/visual.spec.ts-snapshots/
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/desktop/e2e/visual.spec.ts-snapshots
          if git diff --cached --quiet; then
            echo "baselines unchanged"
            exit 0
          fi
          git commit -m "test(e2e): update visual baselines"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "HEAD:refs/heads/${BRANCH}"
        env:
          GITHUB_TOKEN: ${{ github.token }}
          BRANCH: ${{ inputs.branch }}
```

Security shape (zizmor-relevant): `persist-credentials: false` on every checkout, the branch input reaches shell only through `env`, `contents: write` is scoped to the `commit` job alone, and protected `main` rejects the push by ruleset regardless of input.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/update-visual-baselines.yml
git commit -m "ci: dispatchable visual baseline regeneration

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

actionlint and zizmor have no local runners; the PR's required jobs lint the workflow.

---

### Task 3: ADR-0034 and the index row

**Files:**

- Create: `docs/adr/0034-screen-level-visual-regression.md`
- Modify: `docs/adr/README.md` (one appended row)

**Interfaces:**

- Consumes: the spec's decisions.
- Produces: the ADR the meta-gate requires for workflow-file changes.

- [ ] **Step 1: Write the decision record**

`docs/adr/0034-screen-level-visual-regression.md`, exactly:

```markdown
# 0034: Screen-level visual regression on the real Electron shell

**Status**: Accepted
**Date**: 2026-07-25

## Context

Chromatic (Architecture Decision Record (ADR) 0033) snapshots every Storybook story, but stories render components and pages in isolation against the fake bridge. Nothing captured the composed application: the router shell, the sidebar, and the pages inside the real Electron window. The maintainer set the platform decision directly. The app ships to macOS, Windows, and Linux, platform-specific component customization is certain on the roadmap, and the visual layer therefore runs the full three-platform matrix from day one. Playwright's guidance settles where baselines come from: screenshots must come from the environment where the comparisons run, so the continuous-integration runners are the only source of truth for every platform, including macOS.

## Decision

- **A `visual` project joins the end-to-end suite as a plain Playwright spec.** A pixel comparison is a technical proof, not business behavior, the same line ADR-0031 drew for the boot proof and the packaged smoke. The spec reuses the launch fixture with its fresh user-data seam and asserts `toHaveScreenshot` on three screens: the home empty state, the providers empty state, and the providers list after connecting an account.
- **A `snapshotPathTemplate` forces a `{platform}` suffix on every snapshot.** Playwright doesn't suffix explicitly named snapshots, so without the template the three platforms would fight over one file. Nine images exist at the start: three screens across darwin, win32, and linux.
- **Baselines live in plain git and come only from continuous integration.** The `update-visual-baselines.yml` workflow is the single producer: the maintainer dispatches it with a branch name, three matrix legs regenerate their platform's images, and one collector job commits every refreshed image to the branch in a single conventional commit. The maintainer's machine never produces a committed baseline, because even a macOS laptop renders differently from the macOS runner. Git Large File Storage stays out until the snapshot directory grows past 25 MB or measurably hurts clone times.
- **The tolerance is explicit and starts at zero.** `maxDiffPixels: 0` sits in the Playwright config. Every baseline and every comparison come from the same runner images, so zero is the honest starting point. A raise needs observed continuous-integration evidence recorded here, the same ratchet discipline ADR-0015 set.
- **The assertions run inside the required `e2e` job on all three legs.** The Linux leg runs inside the existing dbus and keyring session, because the connected-account screen exercises the vault. The gate stays transitively required through `ci-success` with no ruleset change, and the ADR-0031 quarantine policy applies unchanged.
- **The bot-commit path holds `contents: write` on the collector job alone.** Every checkout keeps `persist-credentials: false`, the branch input reaches the shell only through the environment, and protected `main` rejects any direct push by ruleset.
- **Chromatic's Playwright product stays rejected for this layer.** It documents no Electron support, and it re-renders archives in a cloud Chrome. That re-render tests a different artifact from the one users run, which ADR-0031's packaged-smoke rationale already forbids.

## Alternatives

- **Ubuntu-only visual assertions**: rejected by maintainer directive. The app ships to three platforms and platform-specific component customization is certain, so a single-leg gate would go blind exactly where divergence lands.
- **Chromatic's Playwright integration**: rejected. No documented Electron support, and its cloud re-render violates the test-the-shipped-artifact principle.
- **Locally generated baselines**: rejected. Playwright's own guidance requires baselines from the comparison environment, and Windows and Linux images can't come from the maintainer's machine at all.
- **Git Large File Storage from day one**: rejected. Nine window-sized images don't justify the tooling; the revisit trigger is recorded above.
- **Gherkin scenarios for the screenshots**: rejected. ADR-0031 reserves `.feature` files for business behavior; a pixel diff is a technical proof.

## Consequences

**Good**: a change that breaks the composed shell now fails a required check on the exact platform it breaks, with expected, actual, and diff images attached as artifacts. Platform-specific UI work gets its baselines through the same workflow the day it lands. Refreshing baselines after an intentional change costs one dispatch and one reviewed diff.

**Bad, and accepted**: runner image updates can shift rendering and churn baselines; the recovery is one dispatch plus a reviewed commit. Committed images grow the repository over time, bounded by the Large File Storage trigger. The zero tolerance may prove too strict on some leg; the first raise must carry its evidence into this record.
```

- [ ] **Step 2: Append the index row**

In `docs/adr/README.md`, after the 0033 row:

```markdown
| [0034](0034-screen-level-visual-regression.md) | Screen-Level Visual Regression on the Real Electron Shell, Three-Platform Baselines | Accepted | 2026-07-25 |
```

Run `pnpm run fmt` then `pnpm run fmt:check`.

- [ ] **Step 3: Run the prose gates**

Run: `pnpm run lint:prose && pnpm run lint:spell`
Expected: 0 errors. (`visual` in headings is covered by the committed `[Vv]isual` vocabulary entry; spell out ADR and Inter-Process Communication style acronyms on first use as the text above already does.)

- [ ] **Step 4: Commit**

```bash
git add docs/adr/0034-screen-level-visual-regression.md docs/adr/README.md
git commit -m "docs(adr): screen-level visual regression record

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: PR, baseline seed, and green verification

Runs after Tasks 1–3 are committed. GitHub operations only; no new files except the bot's baseline commit.

**Interfaces:**

- Consumes: the pushed branch; the `update-visual-baselines.yml` workflow.

- [ ] **Step 1: Push and open the PR**

```bash
git push -u origin worktree-e2e-visual
gh pr create --title "test(e2e): screen-level visual regression gate" --body "$(cat <<'EOF'
## Summary

- visual project asserts three shell screens (home empty, providers empty, providers connected) with toHaveScreenshot on all three e2e legs
- snapshotPathTemplate forces per-platform baselines (nine PNGs, plain git); maxDiffPixels starts at 0
- update-visual-baselines.yml regenerates baselines in CI per platform and bot-commits them to the branch (contents: write on the collector job only)
- baselines are CI-only by design: even macOS images come from the runner, never a laptop
- ADR-0034 records the three-platform directive, the tolerance ratchet, the workflow shape, and the Chromatic-Playwright rejection

## Test plan

- [ ] update-visual-baselines dispatched on this branch seeds nine baselines via bot commit
- [ ] all three e2e legs pass the visual project against the seeded baselines
- [ ] existing acceptance, proofs, leak, and packaged projects stay green
- [ ] tolerance evidence recorded in ADR-0034 if any leg needs a raise

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

The first CI run's e2e legs FAIL on the visual step (no baselines exist) — expected; note it on the PR if CodeRabbit asks.

- [ ] **Step 2: Dispatch the seed**

```bash
gh workflow run update-visual-baselines.yml -f branch=worktree-e2e-visual
gh run list --workflow update-visual-baselines --limit 1
gh run watch <run-id>
```

Expected: three regenerate legs pass, the commit job pushes one `test(e2e): update visual baselines` commit with nine PNGs.

- [ ] **Step 3: Pull the bot commit and verify CI goes green**

```bash
git pull --rebase origin worktree-e2e-visual
gh run list --branch worktree-e2e-visual --workflow ci --limit 1
```

Watch the fresh ci run: all three e2e legs must pass the visual step. If exactly one leg flakes on pixel noise, raise `maxDiffPixels` to the smallest passing value observed in the diff output, update the ADR's tolerance bullet with the number and the run link, commit both together (`test(e2e): raise visual tolerance with ci evidence`), push, and re-dispatch the update workflow if baselines need a refresh.

- [ ] **Step 4: Verify the nine baselines and the mechanism twice**

```bash
git ls-files apps/desktop/e2e/visual.spec.ts-snapshots | sort
```

Expected: exactly nine files, three per platform suffix. The update workflow has now run at least once; if Step 3 required tuning, it ran twice, satisfying the spec's exercise requirement. If it only ran once, dispatch it once more and confirm the commit job reports "baselines unchanged" — that second dispatch proves the no-op path.

- [ ] **Step 5: CodeRabbit and merge flow**

Standard project flow: judge findings against docs and code, reply and resolve threads per CLAUDE.md, keep CI green. Merge is the maintainer's admin bypass.
