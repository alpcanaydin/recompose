pre-commit:
  jobs:
    - name: gitleaks
      priority: 0
      run: command -v gitleaks >/dev/null || brew install gitleaks; gitleaks git --staged --redact --no-banner
    - name: lint
      priority: 1
      glob: '*.{ts,tsx,js,jsx,mjs,cjs}'
      run: ./node_modules/.bin/oxlint --fix --deny-warnings --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    - name: fmt
      priority: 2
      glob: '*.{ts,tsx,js,jsx,mjs,cjs,json,md,yaml,yml,html,css}'
      run: ./node_modules/.bin/oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    - name: typecheck
      priority: 3
      run: pnpm run typecheck

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm exec commitlint --edit {1}
