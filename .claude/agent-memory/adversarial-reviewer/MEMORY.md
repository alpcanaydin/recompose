# Memory index

- [Hook exit-code fail-open](hook-exit-code-fail-open.md) — only exit 2 blocks; check every launch precondition and every entry guard's runtime version claim against declared engines
- [Gate scope anchored to session root](gate-scope-anchored-to-session-root.md) — probity anchors globs to the loaded config's directory; the payload-path walk closed the worktree gap and widened whose code runs
