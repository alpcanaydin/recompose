# Memory index

- [Hook exit-code fail-open](hook-exit-code-fail-open.md) — only exit 2 blocks; check every launch precondition and every entry guard's runtime version claim against declared engines
- [Gate scope anchored to session root](gate-scope-anchored-to-session-root.md) — the Edit-tool, agent_id, and agent_id-plus-absolute rewriting shapes are closed, each measured at 1 red; measure from git archive, not the shared tree
