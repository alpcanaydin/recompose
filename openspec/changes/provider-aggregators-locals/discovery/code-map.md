# Code map

- `packages/contracts/src/accounts.ts` (contracts package (cross-process, outside FSD)) — symbols: ACCOUNTS_VERSION, accountKindSchema, AccountKind, credentialedAccountKindSchema, CredentialedAccountKind, CredentialedAccount, Account, accountsDocumentSchema, loadAccountsDocument, defaultAccountsDocument. The registry contract: `aggregator` already sits in `credentialedAccountKindSchema`, but no union member admits `kind: 'local'`, so a credential-free local shape plus a version-4 migration lands here.
- `packages/contracts/src/ipc.ts` (contracts package (cross-process, outside FSD)) — symbols: ipcChannels, connectAccountRequestSchema, IpcChannel, IpcRequest, IpcResponse, RecomposeIpc, ipcResult. Every channel and its schemas; detection needs a new entry beside `accounts:check-key`, and `connectAccountRequestSchema` requires a `secret` a local runtime has none of.
- `packages/contracts/src/api-keys.ts` (contracts package (cross-process, outside FSD)) — symbols: keyProviderIdSchema, KeyProviderId, pastedKeySchema, keyTail, vendorShapeOf, keyCheckVerdictSchema, KeyCheckVerdict, keyCheckReportSchema. The verdict-and-report shape a reachability reading copies, and the enum that deliberately omits openrouter, which is why an aggregator names no host and offers no Verify.
- `packages/contracts/src/migration.ts` (contracts package (cross-process, outside FSD)) — symbols: Migration, migrateDocument. Where the version-4 accounts bump registers once a local account shape exists.
- `packages/contracts/src/index.ts` (contracts package barrel (outside FSD)) — symbols: none. The one public surface of the contracts package, so any new local-runtime module re-exports through it.
- `packages/engine/src/provider/key-probe.ts` (engine package (outside FSD)) — symbols: probeKey, firstPartyProbeOrigins. The fetch-with-`AbortSignal.timeout` probe a localhost look copies, bound today to origins keyed by `KeyProviderId`.
- `apps/desktop/src/main/ipc/key-check-ipc.ts` (main process, IPC handlers (outside FSD)) — symbols: KeyCheckIpcContext, createKeyCheckIpcHandlers. Turns a probe into a report over IPC; the local-runtime detection handler mirrors its shape.
- `apps/desktop/src/main/ipc/connect-account.ts` (main process, IPC handlers (outside FSD)) — symbols: connectAccount. Mints a `credentialRef` and writes a vault secret on every connect, so a credential-free local add needs a path that skips the vault entirely.
- `apps/desktop/src/main/ipc/storage-ipc.ts` (main process, IPC handlers (outside FSD)) — symbols: createStorageIpcHandlers, StorageIpcHandlers. Serves `accounts:connect` and `accounts:remove`, so a local add and a local removal wire in here.
- `apps/desktop/src/main/ipc/register-ipc.ts` (main process, IPC handlers (outside FSD)) — symbols: registerIpcHandlers. The single registration point every new channel passes through.
- `apps/desktop/src/main/storage/accounts-store.ts` (main process, storage (outside FSD)) — symbols: loadAccountsFile, saveAccountsFile, amendAccountsFile. Reads and amends the accounts document a stored local runtime joins, carrying its address and no secret.
- `apps/desktop/src/main/storage/vault.ts` (main process, storage (outside FSD)) — symbols: VaultDocument, setSecret, getSecret, deleteSecret. Named to mark the boundary the local path must not cross, because the spec forbids asking for a credential.
- `apps/desktop/src/preload/index.ts` (preload bridge (outside FSD)) — symbols: none. Forwards every `ipcChannels` key to the renderer, so a detection channel reaches the window only after it is listed here.
- `apps/desktop/src/preload/index.d.ts` (preload bridge (outside FSD)) — symbols: none. The typed window surface stories and browser tests fake against.
- `apps/desktop/src/renderer/src/entities/account/model/account-kind.ts` (entities) — symbols: AccountKind, accountKinds, offeredAccountKind, accountKindTitle, accountsOfKind. Already titles the two destinations Aggregators and Local Runtimes; `accountsOfKind` feeds the sidebar counts, which begin counting local rows the moment the contract stores them.
- `apps/desktop/src/renderer/src/entities/account/index.ts` (entities (public API)) — symbols: none. The slice barrel every consumer of the account kinds imports through.
- `apps/desktop/src/renderer/src/pages/providers/model/provider-catalog.ts` (pages) — symbols: catalogEntries, CatalogEntry, CatalogOffer, ConnectionWay, AwaitedProvider, awaitedFor, offerFor, offeredUnder, providerName, keyTitleFor, keyHostFor, keyShapeHintFor, checkableKey, markFor, keyKindOf, signInProviderOf, subscriptionTitleFor. The center of both halves: OpenRouter already stands as an `aggregator` offer, the four runtimes sit inert in the awaited locals list, the awaited map gives `aggregator` an empty list, and `ConnectionWay` excludes `local` by construction.
- `apps/desktop/src/renderer/src/pages/providers/ui/providers-page/providers-page.tsx` (pages) — symbols: ProvidersPage. Picks a surface per kind and per-kind subtitle copy; its local branch routes to the placeholder note and must swap to a real surface.
- `apps/desktop/src/renderer/src/pages/providers/ui/local-runtimes-note/local-runtimes-note.tsx` (pages) — symbols: LocalRuntimesNote. The placeholder that says local runtimes arrive later, which this feature retires.
- `apps/desktop/src/renderer/src/pages/providers/ui/catalog-flow/catalog-flow.tsx` (pages) — symbols: CatalogFlow, CatalogFlowProps. The grid-then-connect flow; its `connectStepFor` returns nothing for `local`, so a picked runtime currently dead-ends before any detect step.
- `apps/desktop/src/renderer/src/pages/providers/ui/catalog-list/catalog-list.tsx` (pages) — symbols: CatalogList. Renders connectable cards plus the inert Soon-badged awaited cards, and special-cases `local` to awaited rows only.
- `apps/desktop/src/renderer/src/pages/providers/ui/credentialed-surface/credentialed-surface.tsx` (pages) — symbols: CredentialedSurface. Already lists stored `aggregator` rows off the accounts query; the local surface is its sibling without the credential.
- `apps/desktop/src/renderer/src/pages/providers/ui/key-account-row/key-account-row.tsx` (pages) — symbols: KeyAccountRow. The two-line key anatomy the aggregator row reuses, and the place `checkableKey` already withholds Verify from an aggregator.
- `apps/desktop/src/renderer/src/pages/providers/ui/connect-key-form/connect-key-form.tsx` (pages) — symbols: ConnectKeyForm. The name-and-key form OpenRouter reuses unchanged, where `keyHostFor` returning nothing already suppresses the host line for an aggregator.
- `apps/desktop/src/renderer/src/pages/providers/ui/provider-connect-way/provider-connect-way.tsx` (pages) — symbols: ProviderConnectWay. The fork sending a pick to the sign-in half or the key half; a detect-then-add half joins it for local runtimes.
- `apps/desktop/src/renderer/src/pages/providers/ui/credentialed-empty-state/credentialed-empty-state.tsx` (pages) — symbols: CredentialedEmptyState. The empty state for a credential kind, already carrying an Aggregators story.
- `apps/desktop/src/renderer/src/pages/providers/ui/kind-empty-state/kind-empty-state.tsx` (pages) — symbols: KindEmptyState. The title-and-explanation shell an empty Local Runtimes surface reuses once the placeholder note goes.
- `apps/desktop/src/renderer/src/pages/providers/ui/picked-identity/picked-identity.tsx` (pages) — symbols: PickedIdentity. The picked-product header a detection step reads under, matching the key form.
- `apps/desktop/src/renderer/src/pages/providers/ui/add-provider-act/add-provider-act.tsx` (pages) — symbols: AddProviderAct. The one way into the catalog, kind-locked from the window strip.
- `apps/desktop/src/renderer/src/pages/providers/ui/provider-catalog-sheet/provider-catalog-sheet.tsx` (pages) — symbols: ProviderCatalogSheet. Wraps the flow as a sheet over whichever destination opened it.
- `apps/desktop/src/renderer/src/pages/providers/index.ts` (pages (public API)) — symbols: none. Exports only `AddProviderAct` and `ProvidersPage`, so any new surface stays internal to the slice.
- `apps/desktop/src/renderer/src/shared/api/accounts.ts` (shared) — symbols: accountsQueryOptions, useConnectAccount, useVerifyKey, useRemoveAccount. The accounts query and its mutations; a reachability read and a credential-free connect land beside them.
- `apps/desktop/src/renderer/src/shared/api/ipc-result.ts` (shared) — symbols: IpcResultError, refusalSentence, withRefusal, unwrapIpcResult. Turns an IPC refusal into a sentence a row can print, which a detection failure reuses.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared (segment public API)) — symbols: none. The api segment barrel every page imports queries and mutations through.
- `apps/desktop/src/renderer/src/shared/ui/brand-mark/brand-mark.tsx` (shared) — symbols: BrandMark, BrandMarkName. Holds marks for anthropic, openai, and openrouter only, and `CatalogEntry.id` is typed to it, so the four runtimes have no catalog identity yet.
- `apps/desktop/src/renderer/src/shared/ui/icon/icon.tsx` (shared) — symbols: Icon, IconName. Carries the `monitor` and `network` glyphs the runtime rows and awaited cards lead with.
- `apps/desktop/src/renderer/src/shared/ui/badge/badge.tsx` (shared) — symbols: Badge. Renders the Soon badge the aggregators spec requires on contract-less entries.
- `apps/desktop/src/renderer/src/shared/ui/status-chip/status-chip.tsx` (shared) — symbols: StatusChip. A word-and-tone chip, the standing candidate for a row reading running or unreachable.
- `apps/desktop/src/renderer/src/shared/ui/status-indicator/status-indicator.tsx` (shared) — symbols: StatusIndicator. The dot form of a standing, the other candidate for a runtime row's live reading.
- `apps/desktop/src/renderer/src/shared/ui/text-field/text-field.tsx` (shared) — symbols: TextField. The field the key form and any address field are built from.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared (segment public API)) — symbols: none. The ui segment barrel; a new shared control must be listed here to reach the pages.
- `apps/desktop/src/renderer/src/shared/testing/fake-accounts.ts` (shared) — symbols: accountHandlers. The accounts half of the fake bridge stories and browser tests seed through, so it needs local rows and a detection answer.
- `apps/desktop/src/renderer/src/shared/testing/fake-bridge.ts` (shared) — symbols: installFakeBridge, BridgeParameters, gatewaySeed, GatewaySeed, emitEngineStates. Installs the fake window bridge; a detection channel needs a seedable answer in its parameters.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/ui/provider-sidebar/provider-sidebar.tsx` (widgets) — symbols: ProviderSidebar. One row per kind with a live count, so the Local Runtimes row stops reading zero once local accounts store.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/index.ts` (widgets (public API)) — symbols: none. The sidebar slice barrel the app shell imports through.
- `apps/desktop/src/renderer/src/app/routes/providers.tsx` (app) — symbols: Route. Narrows the `?kind` search param to an offered kind, so both destinations are already routable without change.
- `apps/desktop/e2e/provider-screen.ts` (e2e helpers (outside FSD)) — symbols: keyCatalogEntries, catalog, catalogEntry, screenTitle, accountRows, accountRow, nameField, keyField, keyVerdict, openKeysScreen, openSubscriptionsScreen, openCatalog, openProviderWays, pickKeyEntry. The page-object helpers new feature files reuse; an aggregators opener and a local-runtimes opener join them.
- `apps/desktop/e2e/features/providers/catalog.feature` (e2e specs (outside FSD)) — symbols: none. Asserts the count of inert Soon rows, which shifts when the four runtimes become connectable.
- `apps/desktop/e2e/steps/providers-catalog.steps.ts` (e2e steps (outside FSD)) — symbols: none. Binds the inert-row count assertions the catalog change invalidates.
- `apps/desktop/e2e/key-probe-stub.ts` (e2e harness (outside FSD)) — symbols: KeyProbeStub, fakeKeyProbe. The stub server standing in for a provider probe, the pattern a localhost runtime stub copies.
- `apps/desktop/.storybook/providers-column.tsx` (storybook decorators (outside FSD)) — symbols: inProvidersColumn. The column decorator every providers story renders inside, needed by the new local-runtime stories.
- `openspec/changes/provider-aggregators-locals/specs/aggregators/spec.md` (openspec change delta) — symbols: none. Two ADDED requirements: OpenRouter connects as an aggregator key, and an aggregator row claims no host and offers no check.
- `openspec/changes/provider-aggregators-locals/specs/local-runtimes/spec.md` (openspec change delta) — symbols: none. Two ADDED requirements: detection precedes adding, and a row reads standing as an observation the registry never stores.
- `openspec/changes/provider-aggregators-locals/manifest.md` (openspec change manifest) — symbols: none. Records tier full, phase discovery, and the branch, and names the API Keys anatomy the aggregator form reuses.
