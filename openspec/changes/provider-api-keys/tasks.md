## Implementation tasks

Five tasks. Task 1 runs first and alone, because every other layer imports what it exports. Tasks 2, 3, and 4 wait on task 1 and then run together, because each owns a tree disjoint from the other two. Task 5 waits on tasks 2, 3, and 4, because it drives the app they assemble.

Every task opens with a named failing test, captures the red run it started from, and drives it to green. Test code changes if and only if behavior changes.

- [ ] **Task 1: contracts.** Owns `packages/contracts/src/`. Depends on nothing, and runs alone: tasks 2, 3, and 4 all import what it hands off.
  - [ ] Opens red in `api-keys.test.ts`: a key pasted with a trailing newline parses to its trim, before the module exists.
  - [ ] `api-keys.ts` lands as the shared key vocabulary, mirroring `subscriptions.ts`: the two provider ids, `pastedKeySchema`, `keyTail()`, `vendorShapeOf()`, and the three verdicts with the key-check report.
  - [ ] Unit specs with fast-check properties pin the vocabulary. The parse trims. An interior control character refuses with a sentence about the key's contents. The tail holds the trim's last four characters and stays absent for trims of eight or fewer. The shape hint knows the one documented `sk-ant-` family and stays silent otherwise.
  - [ ] The accounts document moves to version 3: the credentialed arm gains the optional `keyTail`, and the migration restamps the version and touches no row. A property proves any version 2 document migrates with every row byte-identical. `accounts.test-d.ts` pins the tail optional and pins that no arm carries a `secret` or `key` property.
  - [ ] The engine protocol gains the probe directive arm, and the report becomes a discriminated union with the key-check arm. Admission and refusal specs ride in `engine-protocol.test.ts`. A new type spec pins the union discriminating on `kind`, with no key-check field beyond the verdict, the status, and the answered id.
  - [ ] The channel surface gains `accounts:check-key`, and `connectAccountRequestSchema` swaps its secret for `pastedKeySchema`, so every caller passes the trim and the refusal. `ipc.test.ts` moves the roster to twenty-one, and `ipc.test-d.ts` pins totality over twenty-one channels with the error codes held at eleven.
  - [ ] `index.ts` re-exports the module, and every consumer typechecks and passes again.
  - [ ] Layers: unit, property, and type-level.

- [ ] **Task 2: the engine probe.** Owns `packages/engine/src/`: the provider probe pair, `engine-child.ts` with its spec, the pipe-hygiene spec, and the `testing/hygiene-child.mts` entry. Depends on task 1, whose directive and report schemas it reads. Runs beside tasks 3 and 4, each of the three on a disjoint tree.
  - [ ] Opens red in `provider/key-probe.test.ts`: an injected fetch answering 200 folds to the authenticates verdict, before the probe exists.
  - [ ] `provider/key-probe.ts` lands as the pure fetch-injected probe: `GET /v1/models` under each vendor's own auth header, refused redirects, and a bounded call. Unit specs hold the folding table, and a property pins one verdict per integer status with authenticates only on 2xx, so the folding never overclaims. A thrown fetch and a refused redirect fold to could-not-check.
  - [ ] `engine-child.ts` dispatches the probe directive, holds the key in the probe call's function scope, and posts the key-check report. It composes the probe origin from its environment, with the vendors' first-party hosts as the default. `engine-child.test.ts` drives the round trip over the fake parent port.
  - [ ] The child's refusal log sanitizes to issue paths and codes, never received values, so a malformed probe can't print a key. `engine-child-pipe-hygiene.test.ts` forks a real child through `testing/hygiene-child.mts`, feeds it a malformed probe carrying a marker secret, and asserts both pipes stay clean.
  - [ ] Layers: unit, property, and integration over a real forked child.

- [ ] **Task 3: main.** Owns `apps/desktop/src/main/`, `apps/desktop/src/preload/index.ts`, and the record its decisions earn: `docs/adr/0070-key-checks-live-in-the-engine-child.md` with its `docs/adr/README.md` index row. Depends on task 1, with a scripted child standing in for task 2's real one, so it runs beside tasks 2 and 4 on disjoint trees.
  - [ ] Opens red in `storage-ipc.test.ts`: a connect whose name the same provider already holds refuses with `name-conflict` while the vault stays untouched, before the guard exists.
  - [ ] Connect refuses the held name per provider inside the same queued turn that writes the registry, before the vault opens for writing. A connect that stands mints the tail from the trimmed key onto the stored row. The untouched-vault-after-refusal spec stands as the designated mutant killer.
  - [ ] `engine-host.ts` gains the `probe` method and routes reports by kind. One spec holds the relation: the host's probe wait bound stands above the child's fetch bound. A dead child and a host timeout fold to could-not-check, each with a sanitized log line naming what failed.
  - [ ] `key-check-ipc.ts` lands mirroring `subscriptions-ipc.ts`: the read and decrypt run inside the vault queue, the probe runs outside it, and a missing row or vault entry answers a typed refusal. Handler specs run against temp storage and the scripted child, and answer each of the three verdicts.
  - [ ] `storage-ipc-secret-hygiene.test.ts` proves no check response, no connect response, and no console line carries any window of the key, and that the list path performs no vault read.
  - [ ] `dispatch.ts` registers the twenty-first channel with its totality spec, `index.ts` composes the check handler over the engine host, the preload bridge gains its one entry, and the accounts-store fixture moves to version 3.
  - [ ] Architecture Decision Record (ADR) 0070 lands from the design's decision 3 draft. It rides here because this task executes the custody and mask decisions: main decrypts, one structured-clone message carries the key, and the tail mints at connect time.
  - [ ] Layers: unit, integration, and property.

- [ ] **Task 4: the renderer surface.** Owns `apps/desktop/src/renderer/src/`, plus the records its surface makes stale: the `README.md` endpoint bullet, the amendment to `openspec/specs/subscriptions/spec.md`, and the `cspell-words.txt` vocabulary. Depends on task 1 for types, and runs beside tasks 2 and 3 on disjoint trees.
  - [ ] Opens red in `provider-catalog.test.ts`: `awaitedFor('api-key')` answers seven entries instead of none, before the catalog grows.
  - [ ] The catalog gains the seven awaited entries: Gemini API, Mistral, xAI Grok, DeepSeek, Moonshot AI, Qwen, and Custom endpoint. Each stands inert under a Soon badge, in the `aria-disabled` anatomy the subscription kind ships, and answers neither pointer nor keyboard. Every benefit line names what its entry waits on. `keyTitleFor` answers the product title, with a text fallback for an unknown provider.
  - [ ] `key-account-row.tsx` lands in the sibling row's anatomy. The first line reads the product title. The second reads the name beside the tail, drawn as four bullets and the four characters. The check act appears only where the probe knows the provider, and the overflow carries Remove. A row stored before this change carries no tail, so its second line reads the name alone. An unknown provider renders without a mark and offers remove alone, and an aggregator row takes the anatomy without the check act. The `key-account-row.stories.tsx` and `key-account-row.browser.test.tsx` siblings ship in this task, no-tail and unknown-provider rows included.
  - [ ] `connect-key-form.tsx` gains the required name field and the foreign-shape warning. The warning knows the one `sk-ant-` family, speaks under the OpenAI API entry, and never blocks the connect. A refused connect keeps the draft. The stories and browser-test siblings follow in this task.
  - [ ] `credentialed-surface.tsx` renders the key rows, and the placeholder `account-list.tsx` retires with `account-list.stories.tsx`: deleted, never grown.
  - [ ] `useCheckKey()` lands with no invalidation, because nothing stored changes, and the fake bridge gains the check stub with a scripted verdict seed. `providers-page.browser.test.tsx` proves each verdict sentence speaks as of the check and a remount forgets it.
  - [ ] Everything that reaches the screen gets the `claude-in-chrome` pass in both schemes before the task closes.
  - [ ] The stale records move in the same task. The `README.md` bullet stops promising a base URL and names the two first-party keys. The subscriptions spec sentence saying a key pick asks for the key alone rewrites to ask for a name and a key. `cspell-words.txt` gains DeepSeek, Gemini, Grok, Mistral, Moonshot, xAI, litellm, and openclaw. The whole list rides here, task 3's two citation names included, because the accept list needs one owner while tasks 3 and 4 run together.
  - [ ] Layers: unit, browser, and story.

- [ ] **Task 5: end to end.** Owns `apps/desktop/e2e/`, the visual baselines included. Depends on tasks 2, 3, and 4, because it drives the app they assemble.
  - [ ] Opens red by graduating the four approved feature files from this change's `gherkin/` folder into `e2e/features/providers/`: every scenario fails before the steps and the stub exist.
  - [ ] `fixtures.ts` gains the probe-origin override: a loopback stub serving both vendors' `/v1/models` with scripted statuses, handed through `RECOMPOSE_PROBE_ORIGIN` beside the launcher and keychain overrides. The key still travels only in the directive.
  - [ ] `providers.steps.ts` and `provider-screen.ts` gain the name field, the row lines, the check act, and the verdict sentences, addressed by role and name.
  - [ ] The two scenarios in `accounts.feature` rewrite around the two-field form and the two-line row.
  - [ ] `catalog.feature` graduates its three scenarios: `Adding a provider opens the catalog over the screen`, `An inert entry answers neither pointer nor keyboard`, and `One act leads into the catalog`. The inertness step asserts more than color and position.
  - [ ] `connect.feature` graduates its six scenarios: `A picked entry asks for a name and a key`, `A name stands once per provider`, `Two providers may share a name`, `A key pasted with a trailing newline connects trimmed`, `A key holding a control character is refused for its contents`, and `A key shaped like another vendor warns and still connects`.
  - [ ] `screen.feature` graduates its five scenarios: `Nothing connected explains what a key is`, `A connected key reads as two lines`, `The mask shows four characters and no vendor prefix`, `The stored secret never reaches the screen`, and `Removing a key removes its secret`.
  - [ ] `verify.feature` graduates its four scenarios: `A key that authenticates says so as of the check`, `A turned-away key reads as not accepted, nothing more`, `An unreachable provider leaves the question open`, and `No answer outlives the screen`.
  - [ ] The providers-connected visual baseline regenerates on all three platforms from the pull-request label, never from a local machine.
  - [ ] Layers: end-to-end and visual.
