# Code map

- `openspec/changes/dialect-translation/specs/dialect-translation/spec.md` (outside FSD: openspec change artifact) — symbols: Requirement: A request translates whole, and no field drops without a trace, Requirement: A response translates whole, including the stops, Requirement: The stream translates event for event. The three ADDED requirements that drive every entry below: request translation with named field fates, response translation with stop-reason mapping, and event-for-event stream translation.
- `openspec/changes/dialect-translation/manifest.md` (outside FSD: openspec change artifact) — symbols: tier, phase, branch. Scopes the work to an engine-internal library of pure functions with no fetch and no wiring, and names the parked gateway-virtual-models change as the future consumer.
- `packages/engine/package.json` (outside FSD: engine package) — symbols: @recompose/engine, exports, ./child, @recompose/contracts, hono. The only package export is ./child, so a translation module stays engine-internal by default and the sole workspace dependency it may reach is @recompose/contracts.
- `packages/engine/src/refusals.ts` (outside FSD: engine package) — symbols: missingModelInAnthropicDialect, missingModelInOpenAiDialect, nonLoopbackClient, requestCarriesOrigin, unservedPath. The existing typed-refusal vocabulary in both dialects, the shape the “refused typed” field fate extends; GAP: its AnthropicRefusal and OpenAiRefusal types are module-private, so a translation refusal needs them exported or a new shape.
- `packages/engine/src/gateway-app.ts` (outside FSD: engine package) — symbols: createGatewayApp, ANTHROPIC_MODEL_PATHS, OPENAI_MODEL_PATHS. The serving path the change leaves untouched; it already routes both dialect path sets to a 404 refusal, so it is the eventual call site rather than a file this change edits.
- `packages/engine/src/gateway-app.test.ts` (outside FSD: engine package) — symbols: OPENAI_MODEL_PATHS. Behavior spec pinning today’s both-dialects 404 answers; it stays green because the library adds no route, and it is the guard that proves the serving path stayed untouched.
- `packages/engine/src/provider/key-probe.ts` (outside FSD: engine package) — symbols: firstPartyProbeOrigins. The precedent for a per-concern subdirectory under packages/engine/src, and the existing per-provider anthropic/openai split the translation pair mirrors.
- `packages/engine/src/engine-child.testkit.ts` (outside FSD: engine package) — symbols: aParent, aLoopbackHolding, fetchAnswering. The only fixture convention in the engine: *.testkit.ts builders excluded from coverage and mutation; GAP: no fixtures directory exists anywhere in the repository, so the fixture-driven tests the manifest asks for land as testkit siblings.
- `packages/engine/vitest.config.ts` (outside FSD: engine package) — symbols: include, coverage, exclude, coverageDefaults. The include glob src/**/*.test.ts admits new translation specs with no config edit, and the exclude list already spares *.testkit.ts from coverage.
- `packages/engine/stryker.config.json` (outside FSD: engine package) — symbols: mutate, coverageAnalysis, thresholds, break. Pure translation functions are node-side logic, so they fall under the diff-scoped mutation gate at break 80 with *.testkit.ts excluded from mutation.
- `vitest.shared.ts` (outside FSD: repo config) — symbols: coverageDefaults. Shared coverage thresholds the engine config spreads, which the new library’s specs must satisfy.
- `packages/contracts/src/api-keys.ts` (outside FSD: contracts package) — symbols: keyProviderIdSchema, KeyProviderId. The closed two-member provider enum ('anthropic', 'openai') that names the dialect pair the translation crosses between.
- `packages/contracts/src/engine-protocol.ts` (outside FSD: contracts package) — symbols: EngineGateway. The gateway shape the engine serves against and the protocol module that already carries keyProviderIdSchema; unchanged here, since the library takes no gateway argument.
- `packages/contracts/src/gateway-config.ts` (outside FSD: contracts package) — symbols: RoutingNode, GatewayConfig, gatewayConfigSchema, loadGatewayConfig. Routing targets carry accountId and providerModel, which is what will pick the destination dialect when the parked consumer resumes; no edit lands here this change.
- `packages/contracts/src/index.ts` (outside FSD: contracts package) — symbols: ./engine-protocol, ./gateway-config, ./api-keys. The single barrel every consumer imports through; the translation shapes only land here if they must cross the package boundary, which this engine-internal change does not require.
- `knip.json` (outside FSD: repo config) — symbols: workspaces, packages/engine, entry, ignoreDependencies. GAP: the engine workspace declares only vitest.mutation.config.ts as entry and package.json exports only ./child, so a library with no consumer this change trips lint:dead unless it gains an entry.
- `.dependency-cruiser.cjs` (outside FSD: repo config) — symbols: engine-no-electron, engine-only-contracts, desktop-not-into-engine, headless-scope. The four boundary rules that govern the new module’s imports: no electron, no workspace dependency beyond contracts, and no static reach from the desktop app.
- `docs/adr/0005-single-port-path-per-gateway.md` (outside FSD: docs) — symbols: Single port, path per gateway, both dialects always, /v1/messages, /v1/chat/completions. The standing decision that every gateway always serves both dialects and the request path disambiguates, which is the reason a two-way translation library exists at all.
- `openspec/specs/engine/spec.md` (outside FSD: openspec living spec) — symbols: Requirement: A model request answers with a typed refusal, Requirement: A gateway serves at the root of its own address. The living engine spec these requirements extend; the typed-refusal requirement is the precedent the translation’s typed refusals inherit, and no requirement here changes while the serving path stays untouched.
- `docs/adr/README.md` (outside FSD: docs) — symbols: 0074-brand-marks-come-from-lobehub-icons.md, Accepted. GAP: the ADR index ends at 0074 and no record covers where the translation library lives or which dialect is canonical, so the decision needs a new record at 0075.
- `steiger.config.ts` (outside FSD: repo config) — symbols: defineConfig, fsd.configs.recommended. The FSD gate over apps/desktop/src/renderer/src; this feature touches no renderer layer (app, pages, widgets, entities, shared all stay as they are), so the gate has nothing new to judge.
