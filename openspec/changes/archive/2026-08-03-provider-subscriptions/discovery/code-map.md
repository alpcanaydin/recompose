# Code map

- `openspec/changes/provider-subscriptions/proposal.md` (openspec change artifact (outside FSD)) — symbols: no symbols cited. The locked scope this map is drawn from: a subscription becomes a managed account, a catalog drawer adds one, and no gateway routes it.
- `openspec/changes/provider-subscriptions/manifest.md` (openspec change artifact (outside FSD)) — symbols: no symbols cited. Records tier full, phase discovery, and the branch, so the specs and tasks artifacts still have to be written before implementation starts.
- `openspec/changes/provider-subscriptions/discovery/technical-research.md` (openspec change artifact (outside FSD)) — symbols: no symbols cited. Names the vendor prohibition, the vault string-versus-record question, and the refresh-ownership conflict that decided the switcher pattern.
- `openspec/changes/provider-subscriptions/discovery/acceptance-references.md` (openspec change artifact (outside FSD)) — symbols: no symbols cited. Supplies the acceptance criteria and the credential-lifecycle defect record that handing login and refresh to the official tool avoids.
- `packages/contracts/src/accounts.ts` (contracts package (outside FSD)) — symbols: ACCOUNTS_VERSION, accountKindSchema, accountsDocumentSchema, AccountsDocument, loadAccountsDocument, defaultAccountsDocument. The stored account row; accountKindSchema carries exactly three kinds today, so the fourth kind the sidebar gains and any plan, signed-in-as, or standing field starts here.
- `packages/contracts/src/accounts.test.ts` (contracts package (outside FSD)) — symbols: no symbols cited. Pins the kind enum and rejects an unknown kind, so widening the enum turns this spec red first.
- `packages/contracts/src/ipc.ts` (contracts package (outside FSD)) — symbols: ipcErrorSchema, IpcError, ipcResult, connectAccountRequestSchema, systemStateSchema, SystemState, ipcChannels, IpcChannel, IpcRequest, IpcResponse, RecomposeIpc, ipcEvents, IpcEvent, IpcEventPayload, RecomposeIpcEvents. connectAccountRequestSchema demands a finished secret at call time, and no channel exists to detect the official tool, list its accounts, or switch the active one.
- `packages/contracts/src/index.ts` (contracts package (outside FSD)) — symbols: no symbols cited. Barrel that re-exports accounts and ipc, so any new subscription module reaches main, preload, and the renderer only through this file.
- `packages/contracts/src/gateway-config.ts` (contracts package (outside FSD)) — symbols: GATEWAY_CONFIG_VERSION, GATEWAY_PORT_RANGE, gatewayPortSchema, gatewaySlugSchema, slugFromName, RoutingNode, gatewayConfigSchema, GatewayConfig, loadGatewayConfig. A routing target holds accountId, which is the link a managed subscription account must be refused, and the reason the screen has to say so up front.
- `packages/contracts/src/migration.ts` (contracts package (outside FSD)) — symbols: Migration, migrateDocument. The schema-version stepper every stored document runs through, the landing point for an accounts-document shape change.
- `packages/contracts/src/non-blank.ts` (contracts package (outside FSD)) — symbols: nonBlankString. The trimmed non-empty string primitive every account and credential field is built from.
- `apps/desktop/src/main/storage/accounts-store.ts` (Electron main process (outside FSD)) — symbols: loadAccountsFile, saveAccountsFile. Reads and atomically writes accounts.json, so a managed subscription row lands through exactly these two functions.
- `apps/desktop/src/main/storage/vault.ts` (Electron main process (outside FSD)) — symbols: VaultDocument, VaultNewerSchemaError, loadVaultFile, saveVaultFile, setSecret, getSecret, deleteSecret. entries is Record<string, string>, which decides whether a managed account keeps a vault entry at all now that the official tool owns the credential.
- `apps/desktop/src/main/storage/safe-storage-codec.ts` (Electron main process (outside FSD)) — symbols: SecretCodec, createSafeStorageCodec. The safeStorage encrypt and decrypt pair every vault write passes through, including the Linux plaintext fallback the screen must disclose.
- `apps/desktop/src/main/storage/vault-order.ts` (Electron main process (outside FSD)) — symbols: inVaultOrder. The single-writer queue that serialises vault writes, which any account switch has to join rather than write beside.
- `apps/desktop/src/main/storage/one-at-a-time.ts` (Electron main process (outside FSD)) — symbols: OneAtATime, oneAtATime. The generic serialisation primitive inVaultOrder is built from, reusable for one switch turn per account.
- `apps/desktop/src/main/storage/vault.test.ts` (Electron main process (outside FSD)) — symbols: no symbols cited. Pins the string-valued entry invariant a record-shaped credential would have to break.
- `apps/desktop/src/main/ipc/storage-ipc.ts` (Electron main process (outside FSD)) — symbols: StorageIpcHandlers, createStorageIpcHandlers. Holds accounts:connect, which mints a credential ref, writes the secret, then appends the row; a subscription connect has no secret to write and needs a different path.
- `apps/desktop/src/main/ipc/storage-context.ts` (Electron main process (outside FSD)) — symbols: StorageIpcContext, StoragePaths, storagePathsFor, openVaultForWrite. The dependency seam handlers receive and the vault.bin plus accounts.json path map that a check for the official tool would extend.
- `apps/desktop/src/main/ipc/storage-envelope.ts` (Electron main process (outside FSD)) — symbols: ipcFailure, storageFailure, openVault. Builds the typed refusal envelope, where a missing official tool becomes a stated reason rather than a broken flow.
- `apps/desktop/src/main/ipc/dispatch.ts` (Electron main process (outside FSD)) — symbols: IpcHandlers, ipcChannelNames, dispatchIpc. ipcChannelNames is a hand-kept list parallel to ipcChannels, so a new subscription channel that skips this file is never registered.
- `apps/desktop/src/main/ipc/register-ipc.ts` (Electron main process (outside FSD)) — symbols: registerIpcHandlers. Binds each channel name to ipcMain.handle behind the trusted-sender check, the registration point for any new channel.
- `apps/desktop/src/main/ipc/sender-trust.ts` (Electron main process (outside FSD)) — symbols: TrustedSender, AllowedOrigins, assertTrustedSender. Guards which frame may invoke IPC, the wall a subscription channel stays behind.
- `apps/desktop/src/main/ipc/storage-ipc-secret-hygiene.test.ts` (Electron main process (outside FSD)) — symbols: no symbols cited. Asserts a secret never leaks into an IPC response, the invariant a listing of managed accounts must keep.
- `apps/desktop/src/main/index.ts` (Electron main process (outside FSD)) — symbols: no symbols cited. Side-effect entry point that assembles every handler set and wires safeStorage, the engine host, and window creation, so a subscription handler set is composed here.
- `apps/desktop/src/main/windows/navigation-policy.ts` (Electron main process (outside FSD)) — symbols: NavigationPolicy, isAllowedNavigation, ExternalOpenDecision, decideExternalOpen. decideExternalOpen already opens https targets only, which is what a plan or billing link needs, and isAllowedNavigation already refuses an embedded sign-in window.
- `apps/desktop/src/main/windows/main-window.ts` (Electron main process (outside FSD)) — symbols: HOME_ROUTE, createMainWindow, showMainWindow, openSettingsSurface, openNewGatewaySurface, openGetStartedSurface. The surface-opening entry points and the only current caller of shell.openExternal, which a Subscriptions surface joins.
- `apps/desktop/src/main/windows/permission-policy.ts` (Electron main process (outside FSD)) — symbols: allowsPermission. The one-permission allowlist this change must leave untouched, since delegating login to the official tool needs no web permission.
- `apps/desktop/src/preload/index.ts` (Electron preload (outside FSD)) — symbols: no symbols cited. Freezes one bridge entry per channel onto window.recompose, so every new subscription channel needs an explicit line here.
- `apps/desktop/src/preload/index.d.ts` (Electron preload (outside FSD)) — symbols: no symbols cited. Declares window.recompose and window.recomposeEvents from the contracts types, the only main-side surface the renderer may reach.
- `apps/desktop/src/renderer/src/entities/account/model/account-kind.ts` (entities) — symbols: AccountKind, accountKinds, accountKindTitle, accountsOfKind. The renderer's one authority on the kinds, reading its options off accountKindSchema, so the fourth kind and any subscription-specific label starts here.
- `apps/desktop/src/renderer/src/entities/account/index.ts` (entities) — symbols: AccountKind, accountKindTitle, accountKinds, accountsOfKind. Public API of the account slice, consumed by both the providers page and the provider sidebar widget.
- `apps/desktop/src/renderer/src/entities/account/model/account-kind.test.ts` (entities) — symbols: no symbols cited. Behaviour spec pinning the kind titles and the kind filter, the contract a new kind or a renamed title changes.
- `apps/desktop/src/renderer/src/pages/providers/ui/providers-page.tsx` (pages) — symbols: ProvidersPage. The flat list over a form that the Subscriptions screen replaces; it already carries the max-column, page-inset shell the new screen keeps.
- `apps/desktop/src/renderer/src/pages/providers/ui/account-list.tsx` (pages) — symbols: AccountList. Renders label, provider, kind, and a remove button per row, which the new row shape (mark, name with plan, account, what it serves, standing, overflow) supersedes.
- `apps/desktop/src/renderer/src/pages/providers/ui/connect-account-form.tsx` (pages) — symbols: ConnectAccountForm. Types its draft as IpcRequest<'accounts:connect'>, so a secret-less subscription connect breaks this form's assumption directly.
- `apps/desktop/src/renderer/src/pages/providers/ui/account-kind-field.tsx` (pages) — symbols: AccountKindField. The kind selector the catalog drawer and its provider fork replace as the way a person picks what to connect.
- `apps/desktop/src/renderer/src/pages/providers/index.ts` (pages) — symbols: ProvidersPage. Public API of the providers page slice, the only import the route may use, so a new screen export lands here.
- `apps/desktop/src/renderer/src/pages/providers/ui/providers-page.browser.test.tsx` (pages) — symbols: no symbols cited. Existing behaviour spec for the providers surface, where the red test for the Subscriptions screen starts.
- `apps/desktop/src/renderer/src/pages/providers/ui/account-kind-field.browser.test.tsx` (pages) — symbols: no symbols cited. Behaviour spec for the kind selector, which changes or retires with the catalog drawer.
- `apps/desktop/src/renderer/src/pages/providers/ui/providers-page.stories.tsx` (pages) — symbols: Loaded. Existing story for the providers surface; the heading, subtitle, and empty state need their own story siblings before the branch leaves the machine.
- `apps/desktop/src/renderer/src/pages/providers/ui/account-list.stories.tsx` (pages) — symbols: Populated, Empty. Stories for the populated and empty list, the visual record a lapsed-authorisation row state joins.
- `apps/desktop/src/renderer/src/pages/providers/ui/connect-account-form.stories.tsx` (pages) — symbols: Blank. Story for the connect form, which moves or retires when the drawer takes over adding an account.
- `apps/desktop/src/renderer/src/pages/providers/ui/account-kind-field.stories.tsx` (pages) — symbols: Basic. Story for the kind selector, the record of which kinds a person may pick today.
- `apps/desktop/src/renderer/src/pages/settings/ui/settings-page.tsx` (pages) — symbols: SettingsPage. The settings surface whose column width, page inset, and section rhythm the layout contract binds the Subscriptions screen to.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/ui/provider-sidebar.tsx` (widgets) — symbols: ProviderSidebar. One row per kind with a live count, glyph, and tint; the fourth destination the proposal adds lands in this component and its tint map.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/index.ts` (widgets) — symbols: ProviderSidebar. Public API of the provider sidebar widget, imported by the app shell.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/ui/provider-sidebar.browser.test.tsx` (widgets) — symbols: no symbols cited. Behaviour spec for the per-kind rows and counts, which changes the moment a fourth kind joins.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/ui/provider-sidebar.stories.tsx` (widgets) — symbols: NothingConnected, CountsPerKind, CountsHoldTheTrailingLine, TintsClearTheFloor, DarkScheme. Five stories including the dark scheme and the contrast check a fourth tint has to clear too.
- `apps/desktop/src/renderer/src/widgets/gateway/create/ui/create-gateway-sheet.tsx` (widgets) — symbols: CreateGatewaySheet. The only existing modal composition in the renderer, the working reference for how the catalog drawer holds a form, focus, and refusals.
- `apps/desktop/src/renderer/src/shared/api/accounts.ts` (shared) — symbols: accountsQueryOptions, useConnectAccount, useRemoveAccount. The TanStack Query surface over the three account channels, where a list-managed-accounts query and a switch mutation with its invalidation land.
- `apps/desktop/src/renderer/src/shared/api/ipc-result.ts` (shared) — symbols: IpcResultError, refusalSentence, withRefusal, unwrapIpcResult. Turns a failed envelope into a typed error carrying the code, which is how a missing official tool or a lapsed authorisation reaches the row.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared) — symbols: no symbols cited. Segment public API re-exporting accounts, gateways, engine, and ipc-result, so new account hooks must be exported through it.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared) — symbols: no symbols cited. UI kit public API; the chip, the plan badge, the status chip, and the drawer all have to be exported here rather than imported by file path.
- `apps/desktop/src/renderer/src/shared/ui/sheet.tsx` (shared) — symbols: Sheet. The centered Base UI dialog with title, description, body, and footer; the drawer either extends this or is built beside it, which the solution design settles.
- `apps/desktop/src/renderer/src/shared/ui/sheet.stories.tsx` (shared) — symbols: Open. The single story for the modal surface, the sibling a drawer variant has to match.
- `apps/desktop/src/renderer/src/shared/ui/segmented-control.tsx` (shared) — symbols: SegmentedControl. The nearest neighbour to the filter chip and the reason the gap analysis says a chip is a separate component, since a segment picks one of a closed set.
- `apps/desktop/src/renderer/src/shared/ui/status-indicator.tsx` (shared) — symbols: StatusIndicator. Paints a six pixel dot with the state word as its accessible name only, so the dot beside a visible word is the extension this screen needs.
- `apps/desktop/src/renderer/src/shared/ui/gateway-state.ts` (shared) — symbols: stateWord, stateMark. The word and colour pair behind the indicator, holding running and stopped only, with no attention state for a lapsed authorisation.
- `apps/desktop/src/renderer/src/shared/ui/icon.tsx` (shared) — symbols: IconName, Icon. The whole glyph set, holding person, key, cube, and general marks and no provider brand mark, which is the named set the catalog needs.
- `apps/desktop/src/renderer/src/shared/ui/icon.stories.tsx` (shared) — symbols: Sprite, InAButton. The sprite story that enumerates every glyph, so brand marks show up here or they are undocumented.
- `apps/desktop/src/renderer/src/shared/ui/text-field.tsx` (shared) — symbols: TextField. The plain input the drawer's search field builds on.
- `apps/desktop/src/renderer/src/shared/ui/field-group.tsx` (shared) — symbols: FieldGroup. Heading plus grouped card, the section rhythm the layout contract borrows from the settings surface.
- `apps/desktop/src/renderer/src/shared/ui/field-row.tsx` (shared) — symbols: FieldRow. The leading-to-trailing row shape a subscription row extends with a mark, a plan badge, and a status chip.
- `apps/desktop/src/renderer/src/shared/ui/page-error.tsx` (shared) — symbols: PageError. The route-level error component every providers route already installs.
- `apps/desktop/src/renderer/src/app/routes/providers.tsx` (app) — symbols: Route. Validates the kind search param against accountKindSchema and warms the accounts cache in its loader; a Subscriptions route registers beside or in place of it.
- `apps/desktop/src/renderer/src/app/routes/-app-shell.tsx` (app) — symbols: AppSidebar, AppToolbar, AppContent. Mounts ProviderSidebar in the standing navigation, so a new provider surface reaches the screen through this shell.
- `apps/desktop/src/renderer/src/app/routeTree.gen.ts` (app) — symbols: routeTree, FileRoutesByFullPath, FileRouteTypes, RootRouteChildren. Generated route registry that must be regenerated if the providers route gains a child or a sibling.
- `apps/desktop/src/renderer/src/app/styles/theme.css` (app) — symbols: no symbols cited. Holds the semantic tokens, including one tint per account kind and a single warning colour, with no chip token and no amber attention pair for a lapsed authorisation.
- `apps/desktop/src/renderer/src/app/styles/primitives.css` (app) — symbols: no symbols cited. The raw scale the semantic tokens draw from, including the orange and yellow steps an attention pair would use.
- `apps/desktop/src/renderer/src/shared/testing/fake-bridge.ts` (shared) — symbols: GatewaySeed, gatewaySeed, BridgeParameters, emitEngineStates, installFakeBridge. The in-memory stand-in for window.recompose used by browser specs and every story, so a new subscription channel is unusable in either until it is faked here.
- `apps/desktop/src/renderer/src/shared/testing/index.ts` (shared) — symbols: emitEngineStates, gatewaySeed, installFakeBridge, BridgeParameters, paintedBox, paintedStyle. Public API of the testing segment, consumed by the Storybook decorator and the app-level render helper.
- `apps/desktop/src/renderer/src/app/testing/render-app.tsx` (app) — symbols: renderAt. Renders the router at a path over a seeded fake bridge, the entry point for a browser spec covering the Subscriptions screen.
- `apps/desktop/.storybook/recompose-bridge.tsx` (Storybook config (outside FSD)) — symbols: withRecomposeBridge. The decorator that installs the fake bridge per story, so a subscription story seeds its accounts through BridgeParameters.
- `apps/desktop/e2e/features/providers/accounts.feature` (end-to-end suite (outside FSD)) — symbols: no symbols cited. The two existing account scenarios, connect and remove, which the drawer flow and the switch action rewrite.
- `apps/desktop/e2e/steps/providers.steps.ts` (end-to-end suite (outside FSD)) — symbols: no symbols cited. Step definitions that drive the connect form by role and name, the harness a catalog drawer scenario reuses.
- `apps/desktop/e2e/fixtures.ts` (end-to-end suite (outside FSD)) — symbols: inheritedEnv, PortSquatter, test, Given, When, Then. The Electron fixture and behaviour bindings every step file imports, including the environment the absence of an official tool would be simulated through.
- `apps/desktop/e2e/visual.spec.ts` (end-to-end suite (outside FSD)) — symbols: no symbols cited. Holds the providers-empty and providers-connected screen baselines that replacing the flat list invalidates on all three platforms.
- `packages/engine/src/refusals.ts` (engine package (outside FSD)) — symbols: missingModelInAnthropicDialect, missingModelInOpenAiDialect, nonLoopbackClient, requestCarriesOrigin, unservedPath. The typed refusal the proposal's opening names, and the boundary this change leaves standing because a subscription cannot become a gateway target.
- `.gitleaks.toml` (repository config (outside FSD)) — symbols: no symbols cited. Carries the forbidden-owner-alias custom rule, the working precedent for the prohibition guard this change adds.
- `lefthook.yml` (repository config (outside FSD)) — symbols: no symbols cited. Holds the protect-main job and the lint lanes, the hook surface a new guard joins so it runs before a commit lands.
- `.claude/workflows/check-stories/check-stories.mts` (repository config (outside FSD)) — symbols: no symbols cited. Compares the branch against main and blocks the push while any ui/ component lacks its story sibling, which every new chip, badge, and drawer must satisfy.
- `steiger.config.ts` (repository config (outside FSD)) — symbols: no symbols cited. Runs the Feature-Sliced Design ruleset over the renderer and will judge the layer of any new slice this feature adds.
- `.dependency-cruiser.cjs` (repository config (outside FSD)) — symbols: no symbols cited. Enforces renderer-isolated, engine-only-contracts, and desktop-not-into-engine, so a subscription module sits on one side of those walls.
- `cspell-words.txt` (repository config (outside FSD)) — symbols: no symbols cited. The committed accept list where new provider brand names have to land in the same diff that uses them.
- `README.md` (documentation (outside FSD)) — symbols: no symbols cited. Line 31 promises OAuth sign-in for Claude and Codex subscriptions, the sentence this change turns false and must rewrite.
- `docs/adr/0010-folder-structure-fsd-and-enforced-boundaries.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. The Feature-Sliced Design record that decides the layer of every new renderer file this feature adds.
- `docs/adr/0016-storage-architecture.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Fixes main as the single writer of the vault and accounts registry, which is where a managed account record has to live.
- `docs/adr/0028-security-baseline.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Holds the navigation guard and the https-only external open that already forbid an embedded sign-in window.
- `docs/adr/0047-gateway-token-vault-and-clipboard.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. The precedent for secret handling, mint in main and mask on the way out, that a managed account row must not contradict.
- `docs/adr/0062-a-schema-version-names-one-shape.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. The rule that decides whether new account fields need a new accounts-document schema version.
- `docs/adr/0044-base-ui-shared-component-base.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Names Base UI as the primitive layer, which decides how the drawer, the chip, and the row overflow are built rather than hand-rolled.
- `docs/adr/0009-two-tier-design-tokens.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. The two-tier token rule that governs adding the attention pair and the chip token the gap analysis names.
- `docs/adr/0043-hig-audit-and-tailwind-rules.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. The Human Interface Guidelines audit lane the new screen and drawer have to clear before they land.
- `docs/adr/0065-view-state-stays-in-the-renderer.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Settles that open-and-closed state for a surface like the catalog drawer stays in the renderer rather than in main.
- `docs/adr/0011-repo-guards-forbidden-alias-and-protected-main.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Records why a rule a machine checks beats a rule in prose, the argument the new prohibition guard extends.
- `docs/adr/0049-stories-guard-runs-before-the-pull-request.md` (architecture decision record (outside FSD)) — symbols: no symbols cited. Fixes the stories guard at pre-push, which is when a missing story for a new chip or badge stops the branch.
- `openspec/specs/gateways/spec.md` (openspec living spec (outside FSD)) — symbols: no symbols cited. The gateway capability whose routing target holds accountId, so the rule that a subscription account is not routable amends this file.
- `openspec/specs/engine/spec.md` (openspec living spec (outside FSD)) — symbols: no symbols cited. Holds the typed-refusal requirement for a model request with no model, the behaviour the Subscriptions screen has to explain rather than change. GAP: the living specs carry only app-icon, development-process, engine, gateways, and settings, so an account or subscription delta creates a new capability rather than amending one.
