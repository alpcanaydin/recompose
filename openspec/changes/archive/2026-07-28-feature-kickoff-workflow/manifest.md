---
tier: standard
phase: verification
approvals:
  - the workflow covers the discovery fan-out and stops at the brainstorm
  - the symbol match takes conditional boundaries rather than a plain word boundary
  - the subagent cap counts dispatches rather than arm table rows
branch: worktree-kickoff-workflow
---
