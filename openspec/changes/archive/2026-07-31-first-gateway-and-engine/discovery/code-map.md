# Code map

- `openspec/changes/first-gateway-and-engine/proposal.md` (outside FSD: openspec change artifact) — symbols: What changes, Impact. The change under discovery, naming the gateway creation flow, the sidebar list, the new engine package, and the empty virtualModels relaxation.
- `openspec/changes/first-gateway-and-engine/specs/gateways/spec.md` (outside FSD: openspec change artifact) — symbols: The empty state invites the first gateway, The creation sheet takes a name and a slug, The sheet previews the address the gateway serves, The sidebar lists gateways with their state, A gateway exists before its first model. Five ADDED requirements that drive every renderer entry below plus the contract relaxation.
- `openspec/changes/first-gateway-and-engine/specs/engine/spec.md` (outside FSD: openspec change artifact) — symbols: One server on the stored port, The first path segment selects the gateway, The health path answers for real, A model request answers with a typed refusal, The engine reports its lifecycle to the app. Five ADDED requirements that drive the new engine package and the lifecycle channel work.
- `packages/contracts/src/gateway-config.ts` (outside FSD: contracts package) — symbols: gatewayConfigSchema, gatewaySlugSchema, GATEWAY_CONFIG_VERSION, loadGatewayConfig, GatewayConfig, RoutingNode. Holds the line the change flips, virtualModels: z.array(virtualModelSchema).min(1), and the slug regex the creation sheet validates against.
- `packages/contracts/src/gateway-config.test.ts` (outside FSD: contracts package) — symbols: configArb, validConfig, loadGatewayConfig validates after migration. Behavior spec for the gateway document; the fast-check arbitrary pins virtualModels to minLength 1 and moves with the relaxation.
- `packages/contracts/src/ipc.ts` (outside FSD: contracts package) — symbols: ipcChannels, ipcErrorSchema, IpcError, ipcResult, RecomposeIpc, IpcChannel, IpcRequest, IpcResponse. The twelve-channel registry and the closed error-code enum; engine start, stop, and status channels plus any engine failure code land here first.
- `packages/contracts/src/ipc.test.ts` (outside FSD: contracts package) — symbols: channelNames, exactly the twelve specified channels exist. Hard-codes the channel roster and its count, so every new engine channel changes this spec.
- `packages/contracts/src/ipc.test-d.ts` (outside FSD: contracts package) — symbols: ipc request contracts, error codes are a closed set the renderer can branch on, bridge surface totality. Type-level spec that enumerates every error code and asserts the bridge covers exactly the channel union.
- `packages/contracts/src/settings.ts` (outside FSD: contracts package) — symbols: settingsSchema, ENGINE_PORT_RANGE, defaultSettings, Settings, withSettingsPatch. Holds enginePort with its 8397 default, the port the engine binds and the sheet preview renders.
- `packages/contracts/src/index.ts` (outside FSD: contracts package) — symbols: ./gateway-config, ./ipc, ./settings. Single barrel every consumer imports through, so a new engine contract module has to be re-exported here.
- `packages/contracts/package.json` (outside FSD: contracts package) — symbols: @recompose/contracts, exports. The only workspace package the engine may depend on, and the template a packages/engine package.json copies.
- `packages/contracts/vitest.config.ts` (outside FSD: contracts package) — symbols: typecheck, coverageDefaults. Node-environment vitest config with typecheck enabled; the engine package needs an equivalent of its own.
- `.dependency-cruiser.cjs` (outside FSD: repo config) — symbols: engine-no-electron, engine-only-contracts, desktop-not-into-engine, headless-scope. GAP: packages/engine does not exist on disk, only these four pre-staged rules; desktop-not-into-engine forbids any static apps/desktop import of the engine, so the main process must reach the engine entry as a spawned utilityProcess path.
- `docs/adr/0002-engine-in-electron-utilityprocess.md` (outside FSD: docs) — symbols: utilityProcess, packages/engine. Fixes the engine as pure TypeScript in packages/engine with zero electron imports, run on Electron's bundled Node in a utilityProcess.
- `docs/adr/0005-single-port-path-per-gateway.md` (outside FSD: docs) — symbols: Single port, path per gateway, both dialects always. The decision the engine spec implements, one server on :8397 with the first path segment selecting the gateway.
- `docs/adr/0010-folder-structure-fsd-and-enforced-boundaries.md` (outside FSD: docs) — symbols: engine-host/, packages/engine, Feature-Sliced Design (FSD) v2.1. GAP: names apps/desktop/src/main/engine-host/ as the utilityProcess spawn plus engine entry home, and that directory does not exist yet; also states packages/engine earns its own ADR when it opens.
- `docs/adr/0014-dependency-boundaries-enforcement.md` (outside FSD: docs) — symbols: lint:boundaries, lint:fsd, no-orphans, no-phantom-deps. Explains why the engine rules exist before the engine does and which gate rejects an undeclared dependency in a new package.
- `docs/adr/0044-base-ui-shared-component-base.md` (outside FSD: docs) — symbols: @base-ui/react, shared/ui/index.ts. Establishes Base UI as the kit base, the source a creation sheet dialog primitive would build on.
- `apps/desktop/src/main/index.ts` (outside FSD: main process) — symbols: assembleIpcHandlers, startRecompose, storedSettings, settingsEffects, applyChosenSettingsNow, onStorageCorrupt. Bootstrap that assembles every IPC handler and applies stored settings at boot; the engine host lifecycle hangs off this file.
- `apps/desktop/src/main/ipc/dispatch.ts` (outside FSD: main process) — symbols: IpcHandlers, ipcChannelNames, dispatchIpc. The hand-maintained channel-name array and the parse-dispatch-parse path every new engine channel passes through.
- `apps/desktop/src/main/ipc/register-ipc.ts` (outside FSD: main process) — symbols: registerIpcHandlers, senderFromEvent. Registers ipcMain.handle for each channel name; carries no push mechanism today.
- `apps/desktop/src/main/ipc/storage-ipc.ts` (outside FSD: main process) — symbols: createStorageIpcHandlers, StorageIpcHandlers, listGateways, saveGateway. Already answers gateways:list and gateways:save, so the creation flow needs no new storage handler, only the engine ones.
- `apps/desktop/src/main/ipc/storage-context.ts` (outside FSD: main process) — symbols: StorageIpcContext, StoragePaths, storagePathsFor, openVaultForWrite. The injected-effects context and the gatewaysDir path an engine host would read gateway documents from.
- `apps/desktop/src/main/ipc/storage-envelope.ts` (outside FSD: main process) — symbols: ipcFailure, storageFailure, openVault. The typed-failure envelope helper an engine start failure reuses, which is why ipcErrorSchema needs a code for it.
- `apps/desktop/src/main/storage/gateway-store.ts` (outside FSD: main process) — symbols: saveGatewayConfig, listGatewayConfigs, loadOneGatewayConfig. One JSON file per slug under the gateways directory, the store the sidebar list and the engine both read.
- `apps/desktop/src/main/storage/gateway-store.test.ts` (outside FSD: main process) — symbols: config, listGatewayConfigs, saveGatewayConfig. Its fixture carries a populated virtualModels array, so the empty-array case needs a scenario here.
- `apps/desktop/src/main/storage/initialize-storage.ts` (outside FSD: main process) — symbols: initializeStorage, StorageState. Boot-time read of settings, accounts, and gateways; the natural source for the port and gateway set an engine start consumes.
- `apps/desktop/src/main/settings/apply-settings.ts` (outside FSD: main process) — symbols: SettingsEffects, applyChosenSettings, applyBootSettings. The effects seam a saved enginePort would travel through to reach a running engine.
- `apps/desktop/src/main/ipc/storage-ipc.test.ts` (outside FSD: main process) — symbols: gateway, gateways:save, gateways:list. Round-trip spec for the gateway channels; its fixture also assumes a non-empty virtualModels array.
- `apps/desktop/src/preload/index.ts` (outside FSD: preload) — symbols: recompose, bridgeEntry. GAP: exposes ipcRenderer.invoke only, so no push path exists for engine lifecycle and the screen must either poll a status channel or this file gains an event surface.
- `apps/desktop/src/preload/index.d.ts` (outside FSD: preload) — symbols: Window, RecomposeIpc. Types window.recompose for the renderer; any bridge surface change lands here too.
- `apps/desktop/src/renderer/src/app/routes/__root.tsx` (app) — symbols: Route, RootLayout, RouterAppContext, NotFound. Owns the sidebar shell whose nav currently holds only static links, the surface that gains the gateway list with status dots.
- `apps/desktop/src/renderer/src/app/routes/index.tsx` (app) — symbols: Route. The / route that renders EmptyState today and needs a loader warming the gateway list to decide between empty state and list.
- `apps/desktop/src/renderer/src/app/routes/gateways.$slug.tsx` (app) — symbols: Route, GatewayCanvasRoute, parseSlug. Validates the slug param through gatewaySlugSchema and delegates to the canvas page; the destination a freshly created gateway navigates to.
- `apps/desktop/src/renderer/src/app/routes/settings.tsx` (app) — symbols: Route, SettingsRoute, focusRequest. The loader pattern to copy, warming query options through context.queryClient.ensureQueryData with PageError as errorComponent.
- `apps/desktop/src/renderer/src/app/routeTree.gen.ts` (app) — symbols: routeTree, FileRoutesByFullPath. Generated by the TanStack Router plugin; regenerates if any route file is added or renamed.
- `apps/desktop/src/renderer/src/app/router.tsx` (app) — symbols: createAppRouter, AppRouterOptions. Hash-history router factory and the Register declaration; the composition point if a page needs both a settings query and a gateway query without a same-layer cross-import.
- `apps/desktop/src/renderer/src/pages/home/ui/empty-state.tsx` (pages) — symbols: EmptyState. The single sentence that becomes the Create your first gateway call to action opening the creation sheet.
- `apps/desktop/src/renderer/src/pages/home/ui/empty-state.stories.tsx` (pages) — symbols: Basic, meta. The required story sibling that lint:stories checks; grows a variant once the call to action arrives.
- `apps/desktop/src/renderer/src/pages/home/index.ts` (pages) — symbols: EmptyState. Slice public API, the only import surface the app layer may reach for; new sheet exports pass through here.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page.tsx` (pages) — symbols: GatewayCanvasPage. The Canvas coming soon placeholder the change replaces or keeps while the gateway becomes real.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/index.ts` (pages) — symbols: GatewayCanvasPage. Slice public API; a new api/ segment holding gateway query options and the save mutation belongs inside this slice and exports through this file.
- `apps/desktop/src/renderer/src/pages/providers/api/accounts.ts` (pages) — symbols: accountsQueryOptions, useConnectAccount, useRemoveAccount. The exact queryOptions plus useMutation plus invalidate shape a gateways api segment copies.
- `apps/desktop/src/renderer/src/pages/settings/api/settings.ts` (pages) — symbols: settingsQueryOptions, useSettingsWriter. GAP: the sheet preview needs the stored enginePort from here, but a pages-to-pages import is a same-layer cross-import the Steiger recommended set rejects, so this query has to move to shared/api or be composed from the app layer.
- `apps/desktop/src/renderer/src/pages/settings/index.ts` (pages) — symbols: settingsQueryOptions, systemQueryOptions, gatewayTokenQueryOptions, SettingsPage. Public API that currently exposes settingsQueryOptions for the route loader only, not for another page slice.
- `apps/desktop/src/renderer/src/pages/settings/ui/engine-port-row.tsx` (pages) — symbols: EnginePortRow. Writes the enginePort the change turns from a stored value nothing reads into the port the engine binds.
- `apps/desktop/src/renderer/src/pages/settings/ui/server-section.tsx` (pages) — symbols: ServerSection, waitingOnEngine. Carries the inert Bind address and Start gateways on launch rows whose Waiting on the engine reason the arriving engine puts in question.
- `apps/desktop/src/renderer/src/shared/api/ipc-result.ts` (shared) — symbols: unwrapIpcResult, IpcResultError. Turns the result envelope into a throw a query boundary catches; every new engine call goes through it.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared) — symbols: ./ipc-result. Segment public API for shared/api, the landing spot if the settings query moves down to break the pages cross-import.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared) — symbols: FieldGroup, FieldRow, NumericField, SegmentedControl, Switch, LabelledTextField, TextField, PageError, placeFocus. GAP: the kit holds no dialog or sheet primitive and no status dot, so the creation sheet and the sidebar row indicator both need new components exported here with story siblings.
- `apps/desktop/src/renderer/src/shared/ui/text-field.tsx` (shared) — symbols: TextField. Base UI Field.Root plus Field.Control input reporting every keystroke, the control the slug field needs for a live preview.
- `apps/desktop/src/renderer/src/shared/ui/labelled-text-field.tsx` (shared) — symbols: LabelledTextField. The visibly labelled variant the name and slug fields in the sheet use.
- `apps/desktop/src/renderer/src/shared/ui/place-focus.ts` (shared) — symbols: placeFocus. Moves focus visibly, which is what the requirement that focus lands on the name field calls for.
- `apps/desktop/src/renderer/src/shared/ui/field-row.tsx` (shared) — symbols: FieldRow, notesFor, statusStyles. Row primitive with alert and note status tones, the shape a slug conflict or format message reuses.
- `apps/desktop/src/renderer/src/shared/testing/fake-bridge.ts` (shared) — symbols: installFakeBridge, BridgeParameters, settingsHandlers, accountHandlers. Stubs gateways:list and gateways:save as always-empty; a real stored-gateway seed and the engine channels have to be added for stories and browser tests.
- `apps/desktop/src/renderer/src/shared/testing/index.ts` (shared) — symbols: installFakeBridge, BridgeParameters. Segment public API the Storybook decorator and browser tests import from.
- `apps/desktop/.storybook/recompose-bridge.tsx` (outside FSD: storybook config) — symbols: withRecomposeBridge. Decorator that installs the fake bridge per story from the bridge parameter, the seam a gateway-seeded story sets.
- `apps/desktop/e2e/features/home/first-launch.feature` (outside FSD: e2e) — symbols: A fresh install greets with the gateway empty state. The one scenario asserting today's empty-state copy, which the call to action replaces.
- `apps/desktop/e2e/steps/app.steps.ts` (outside FSD: e2e) — symbols: the app is on the gateways screen, it offers to select a gateway or create one, processIdOf. Step definitions binding that scenario, including the exact sentence the new copy retires.
- `apps/desktop/e2e/fixtures.ts` (outside FSD: e2e) — symbols: test, inheritedEnv, Given, When, Then, RECOMPOSE_USER_DATA_DIR. Launches Electron against a temporary user-data directory; an engine binding a real port needs port isolation added here so parallel runs do not collide.
- `apps/desktop/e2e/playwright.config.ts` (outside FSD: e2e) — symbols: acceptanceDir, acceptance, visual, proofs. Project split and BDD generation config; new gateway and engine feature files are picked up by the features glob automatically.
- `apps/desktop/e2e/visual.spec.ts` (outside FSD: e2e) — symbols: home-empty.png, pinLightScheme, settleFonts. The home baseline test asserts the old empty-state sentence before screenshotting, so both the assertion and the baselines move.
- `apps/desktop/e2e/visual.spec.ts-snapshots/home-empty-darwin.png` (outside FSD: e2e) — symbols: none. Home screen baseline for macOS that regenerates when the empty state gains its call to action, with linux and win32 siblings alongside it.
- `apps/desktop/vitest.config.ts` (outside FSD: repo config) — symbols: unit, browser, storybook, coverageDefaults. Three-project setup and the coverage exclude list a new main-process engine host entry point may need an entry in.
- `apps/desktop/stryker.config.json` (outside FSD: repo config) — symbols: mutate, thresholds, desktop-main. Mutation gate over src/main, so new engine-host logic in the main process falls under the diff-scoped Stryker run.
- `knip.json` (outside FSD: repo config) — symbols: workspaces, packages/contracts, apps/desktop. Dead-code gate keyed per workspace; a packages/engine workspace entry is needed the day the package opens.
- `steiger.config.ts` (outside FSD: repo config) — symbols: fsd.configs.recommended, defineConfig. The FSD gate over the renderer, run with fail-on-warnings, which is what makes the pages cross-import hazard a blocking error rather than advice.
- `package.json` (outside FSD: repo config) — symbols: lint:boundaries, lint:fsd, lint:stories, lint:dead, typecheck. Root gate scripts; lint:boundaries already scans apps and packages, so the engine rules fire the moment packages/engine gains a file.
- `pnpm-workspace.yaml` (outside FSD: repo config) — symbols: packages/_, apps/_. The glob that admits packages/engine without edit, though the desktop package.json still needs the dependency declaration if it references the engine build output.
- `turbo.json` (outside FSD: repo config) — symbols: build, typecheck, lint, test. Task graph a new package inherits by convention through its own package.json scripts.
