---
tier: standard
phase: discovery
approvals:
  - the standard tier on the classifier's recommendation, because the work stays inside packaging and assets, with one bounded code touch for the Linux window icon
branch: worktree-app-icon
---
