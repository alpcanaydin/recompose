---
tier: standard
phase: implementation
approvals:
  - the standard tier on the classifier's recommendation, because the work stays inside packaging and assets, with one bounded code touch for the Linux window icon
  - gate 1 approved the design document with the design critique folded in, and the maintainer relocked the amended decisions below
  - the small sizes ship a purpose-drawn cream note with a dark contour, because self-stroking the master glyph is arithmetically impossible and a bare blue note reads 2.2 to 1 on a dark taskbar
  - the Icon Composer appearances keep the note constant and vary only the tile, with dark deepening toward the dark band palette and mono standing the note alone
  - the master normalizes to a 1024 canvas with solid fills, because Apple's source rules bar baked opacity and the template's alpha has to tint cleanly
  - the Recompose rename rides this change on same-file ownership, and the homebrew cask template moves with it because the hardcoded app line breaks on the next release
  - gate 2 closed on the maintainer's standing approval after the session's examination, freezing the four feature files as the scenario set
  - the runner pin covers both mac legs, and the continuous-integration Linux leg builds real artifacts so the packaged assertions have something to inspect
  - the palette records seven anchors with fills derived through one pure function, because literals with hidden arithmetic invite drift
  - the packaged lane keeps plain Playwright, so the gherkin stays the review contract and the glass edge and tray tint stay manual checks
  - the raster interface widened at the task 1 replan to carry the renderer's own PNG bytes, deleting the hand-written encoder, after the review proved premultiplied alpha in the shipped ico
  - the maintainer's eyeball pass amended the appearance rule for color only: the note's fills re-derive over the dark backdrop in dark and mono, the geometry never varies, and the watch preview stays out of scope with the target matrix
branch: worktree-app-icon
---
